name: Native storefront

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  native:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build qt6-base-dev qt6-declarative-dev libqt6svg6 qt6-tools-dev-tools qml6-module-qtquick qml6-module-qtquick-controls qml6-module-qtquick-dialogs qml6-module-qtquick-layouts qml6-module-qtquick-window qml6-module-qtquick-templates qml6-module-qtqml-workerscript python3 bubblewrap ffmpeg fontconfig fonts-dejavu-core
      - name: Use the pinned Rust toolchain
        run: rustup show
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Check Rust diagnostics
        run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
      - name: Test Rust protocol boundaries
        run: cargo test --workspace --all-features --locked
      - name: Validate the public catalogue
        run: |
          ./bin/build-repository-catalogue --check
          cargo run --locked --bin omastore-validate -- data/registry.json
          cargo run --locked --bin omastore-validate -- data/repository/catalogue.json
      - name: Configure native app
        run: cmake --preset dev
      - name: Build native app
        run: cmake --build --preset dev --parallel 2
      - name: Check process and Qt startup
        run: ctest --preset dev
      - name: Stage desktop packaging
        run: cmake --install build --prefix "$PWD/build/stage"
      - name: Build the separate synthetic preview
        run: |
          cmake --preset demo
          cmake --build --preset demo --parallel 2
      - name: Exercise native keyboard flows and sizing
        run: ctest --preset demo
      - name: Capture the actual preview window
        env:
          QT_QPA_PLATFORM: offscreen
          QT_QUICK_BACKEND: software
        run: ./build-demo/bin/omastore --demo --screenshot build-demo/discovery.png
      - name: Preserve test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: native-test-results
          path: |
            build/Testing/Temporary/
            build-demo/Testing/Temporary/
            build-demo/discovery.png
          if-no-files-found: ignore

  isolated-media:
    # The worker requires permitted unprivileged user/network namespaces.
    # Keep native compatibility coverage on 24.04; exercise the worker on a
    # supported host without relaxing any runner-wide AppArmor/sysctl policy.
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Install the isolated worker dependencies
        run: sudo apt-get update && sudo apt-get install -y bubblewrap ffmpeg
      - name: Use the pinned Rust toolchain
        run: rustup show
      - name: Verify isolated video processing
        run: cargo test -p omastore-workflow --locked video_normalization_is_isolated_and_strips_metadata -- --ignored
